---
"mentor": major
---

Add the mentor skill: the agent writes no code at all and coaches instead, so the learner types
every line themselves and learns by doing. Reading and tracing stay on the agent's side, running
anything is offered to the learner first, and only authorship is always theirs. Writes no files and
stores no state, so it works in any harness that loads skills.
